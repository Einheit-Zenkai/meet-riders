{
  "name": "meet-riders-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter frontend dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint"
  }
}
