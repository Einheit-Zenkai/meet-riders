"use client";

/**
 * The expired parties sidebar has been retired.
 * This component now renders nothing to avoid referencing the archived table.
 */

export default function ExpiredPanel() {
  return null;
}
