/**
 * Expired party functionality has been removed from the dashboard.
 * This module is intentionally left empty so historical imports compile cleanly
 * until the backend feature is reintroduced.
 */

export {};
