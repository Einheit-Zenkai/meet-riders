import React from 'react';
import { View, Text } from 'react-native';

const MapDisplayView = (props: any): JSX.Element => (
  <View style={{ padding: 16 }}>
    <Text>Map not available on this platform</Text>
  </View>
);

export default MapDisplayView;
